node_modules/
.env
*.local
# Don't commit real revenue PDFs / accounting data
data/*.pdf
!data/example.pdf
cache/
# Multi-tenant store: accounts + encrypted per-client secrets + login sessions
data/users.json
data/clients/
data/sessions.json
# Auto-generated AES master key (encrypts every tenant's secrets) — NEVER commit
data/master.key
docker-compose.override.yml
